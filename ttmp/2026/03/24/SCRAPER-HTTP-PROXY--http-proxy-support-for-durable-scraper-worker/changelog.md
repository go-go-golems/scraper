# Changelog

## 2026-03-24

- Created the `SCRAPER-HTTP-PROXY` ticket
- Documented the current implicit environment-proxy behavior in the HTTP runner
- Wrote the detailed proxy-support architecture and implementation guide
- Added the initial phased task breakdown
- Recorded the initial investigation diary
- Implemented explicit `ProxyURL` support in worker HTTP config
- Added `--http-proxy` to `scraper worker run`
- Updated the HTTP runner to clone the default transport and apply explicit proxy routing
- Added validation and tests for explicit proxy routing plus invalid proxy URLs

## 2026-03-24

- Initial workspace created

- Ticket administratively closed on 2026-04-07 and retained as historical context; follow-on work should use newer focused tickets where they exist.
