// Package workflowmodule implements the safe require("workflow") authoring
// module. Opaque JavaScript handles build canonical workflowv3 IR and compile
// through the Go-owned catalog and compiler without execution or store access.
package workflowmodule
